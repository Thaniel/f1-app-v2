export interface IUser {
    id: string;
    user: string;
    firstName: string;
    lastName: string;
    email: string;
    birthdate: Date;
    country: string;
    isActive: boolean;
    isAdmin: boolean;
}
